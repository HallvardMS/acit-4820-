<?xml version="1.0"?>
<launch>

  <let name="pkg_share"          value="$(find-pkg-share assignment_2)"/>
  <let name="urdf_path"          value="$(var pkg_share)/urdf/robot.urdf.xacro"/>
  <let name="world_path"         value="$(var pkg_share)/worlds/my_world.world"/>
  <let name="bridge_config_path" value="$(var pkg_share)/config/gz_bridge.yaml"/>
  <let name="rviz_config_path"   value="$(var pkg_share)/rviz/display.rviz"/>

  <arg name="headless" default="false" description="Run Gazebo without the GUI client"/>

  <executable cmd="gz sim -s -r $(var world_path)" shell="true" name="gz_server" output="screen"/>

  <executable cmd="gz sim -g" shell="true" name="gz_client" output="screen" unless="$(var headless)"/>

  <node pkg="robot_state_publisher" exec="robot_state_publisher">
    <param name="robot_description" value="$(command 'xacro $(var urdf_path)')"/>
  </node>

  <node pkg="ros_gz_sim" exec="create" 
        args="-topic robot_description -entity perri_auv -z 1.0" 
        output="screen"/>

  <node pkg="ros_gz_bridge" exec="parameter_bridge">
    <param name="config_file" value="$(var bridge_config_path)"/>
  </node>

  <node pkg="rviz2" exec="rviz2" name="rviz2" args="-d $(var rviz_config_path)"/>
  <node pkg="assignment_2" exec="AUVAutonomousController" name="AUVAutonomousController"/>
  

</launch>